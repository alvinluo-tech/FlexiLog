# 需求功能文档 (PRD) - FlexiLog AI 健身记录Web应用

## 1. 项目概述
FlexiLog AI 是一款集“传统健身记录”与“AI智能教练”于一体的响应式Web应用。它不仅提供类似《训记》的高效、纯粹的动作打卡与日志记录体验，更核心的是通过引入大语言模型（LLM），结合用户的身体动态参数与历史训练日志，提供个性化的训练计划生成、动作指导以及数据复盘。

---

## 2. 用户痛点与产品核心价值
* **传统App痛点**：记录繁琐（需要频繁切页）、计划呆板（无法根据用户当天状态或历史表现动态调整）。
* **核心价值**：极致流畅的Web端单页记录体验 + 懂用户的AI教练（省去请私教的高昂成本，让数据真正为训练服务）。

---

## 3. 功能需求矩阵 (Feature Matrix)

### 3.1 核心板块一：基础健身模块（参考“训记”设计）
该模块旨在提供清晰的动作库浏览和无干扰的训练记录。

#### 3.1.1 动作库与分类 (Exercise Library)
* **大部位分组**：系统内置“胸、背、腿、肩、手臂（肱二/肱三）、核心、全身”等大项。
* **动作详情页**：
    * **动作指导图**：支持静态图解或 3-5 秒的循环动图（GIF/MP4格式）。
    * **动作要领**：包含“起始姿势、动作轨迹、呼吸方式、常见错误、核心提示”。
* **自定义动作**：允许用户自行创建动作，选择归属部位并上传备注。

#### 3.1.2 训练记录器 (Workout Logger)
* **模版化训练**：支持创建“Push-Pull-Legs (PPL)”、“胸背循环”等固定计划，点击直接进入记录。
* **单页高效记录**：
    * 采用表格/列表流式布局，支持快捷输入：**组数、重量 (kg)、次数 (Reps)、RPE (主观疲劳感知)**。
    * 一键勾选“完成”，自动触发**组间休息倒计时器**。
    * **历史数据对比**：在当前动作记录框下方，微缩显示该动作上一次训练的重量与次数（用于实现渐进性超负荷）。

---

### 3.2 核心板块二：AI 智能集成模块（差异化亮点）
利用 AI 承载核心的“分析-建议-规划”链路。

#### 3.2.1 AI 智能训练计划生成器
* **多维输入感知**：
    * **身体参数**：性别、年龄、身高、体重、体脂率、伤病史、健身年限。
    * **当前目标**：增肌、减脂、维持、提高绝对力量。
    * **时间预算**：每周训练几天、单次训练时长、每次可练的大部位。
    * **器械条件**：商业健身房（器械齐全）、全哑铃环境、居家自重。
* **AI 计划输出**：一键生成周期性（如 4 周循环）或单次动作组合，包含动作顺序、推荐初始重量/组数/次数，并支持“一键一键应用到今日训练”。

#### 3.2.2 动态日志分析与超负荷推荐
* **周/月度报告**：AI 扫描过去一段时间的训练日志，自动生成分析（例如：“你上周的背部训练容量增长了 12%，但胸部训练由于 RPE 过高可能存在过度训练”）。
* **动态调整建议**：如果用户连续两次某个动作达到瓶颈（无法提升重量或次数），AI 会自动弹出建议（例如：“检测到您的深蹲已连续 3 周卡在 100kg，建议下周进行一次 Deload（减载），或将动作替换为腿举以激活不同肌纤维”）。

---

## 4. 非功能性需求与架构设计

### 4.1 技术栈推荐（Full-Stack）
* **前端**：Next.js (React) + Tailwind CSS + Shadcn UI (提供类似 Bento Grid 的现代感数据看板)。
* **后端/数据库**：Supabase (PostgreSQL) - 天然支持用户鉴权、表格行级安全，适合快速迭代开发。
* **AI 接口**：OpenAI API (GPT-4o) / Gemini 1.5 Pro（配合 Structured Outputs 确保 AI 返回严格的 JSON 格式计划，以便前端渲染）。

### 4.2 交互与体验
* **Mobile-First 响应式**：虽然是 Web 应用，但用户在健身房主要通过手机访问，必须保证移动端的交互如同 Native App 一样流畅（例如：大按钮、易滑动的数字选择器）。
* **离线缓存 (PWA)**：健身房信号通常较差，支持渐进式Web应用（PWA）特性，允许离线记录，联网后自动同步数据至 Supabase。

---

## 5. 模块详细页面与路由规划

| 路由路径 | 页面名称 | 核心交互与功能 |
| :--- | :--- | :--- |
| `/dashboard` | 看板中心 | 展现今日训练卡片、身体参数（体重趋势图）、AI 周报入口。 |
| `/exercises` | 动作库 | 按背/胸/腿等大流派分类的 Grid 网格，支持全局搜索与动作筛选。 |
| `/workout/live`| 训练记录页 | 极简单页，包含计时器、动作组数输入、上一组数据对比。 |
| `/ai-coach` | AI 助手 | 输入身体参数与目标的表单页，点击生成后以直观的卡片流展示 AI 计划。 |
| `/profile` | 个人设置 | 身体静态/动态参数修改、健身房器械偏好配置。 |

---

## 6. 项目演进规划 (Milestones)
* **Phase 1 (MVP - 核心基础)**：完成数据库设计、背/胸/腿动作库搭建、移动端响应式训练记录表单（100% 还原纯粹的记录体验）。
* **Phase 2 (AI 注入)**：集成 AI 接口，实现基于用户表单参数生成首份训练计划，并能成功导入记录页。
* **Phase 3 (数据闭环)**：打通“记录-分析-再调整”的链路，上线 AI 历史日志分析报告与动态超负荷建议功能。